import 'package:flutter/material.dart';

class ZartAppBar extends StatelessWidget {
  const ZartAppBar({super.key});
  @override
  Widget build(BuildContext context) {
    return AppBar(elevation: 0, backgroundColor: Colors.transparent);
  }
}
